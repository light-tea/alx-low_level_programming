#include <stdio.h>
/**
 * main -prints the alphabet in lowercase, followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char tea;

	for (tea = 'a'; tea <= 'z'; tea++)
	putchar(tea);
	putchar('\n');

return (0);
}
