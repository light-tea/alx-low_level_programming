#include <stdio.h>

/**
 * main - print alphabet in lower and uppercase
 *
 *
 * Return: (0) Success
 */
int main(void)
{
	char a;
{
        char a;
        for (a = 'a' ; a = 'z' ; a++)
{
        putchar(a);
}
        for (a = 'A' ; a = 'Z' ; a++)
{
        putchar(a);
}
        putchar('\n');
        return (0);
}
